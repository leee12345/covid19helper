<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:fresco="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/page_bg"
    android:orientation="vertical">

    <RelativeLayout style="@style/top_bar">

        <ImageView
            android:id="@+id/back_wd"
            android:layout_width="38dp"
            android:layout_height="38dp"
            fresco:srcCompat="@mipmap/back" />

        <TextView
            android:id="@+id/wdhsText"
            style="@style/top_bar_title"
            android:text="个人中心"
            android:textSize="24sp"
            android:textStyle="bold" />
    </RelativeLayout>

    <View
        android:id="@+id/my_view"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="@drawable/top" />

    <ListView
        android:id="@+id/wd_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:entries="@array/wd"
        android:textFilterEnabled="false"></ListView>
</LinearLayout>